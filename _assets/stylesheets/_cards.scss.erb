.card-section {
  @include make-sm-column(10);
  @include make-sm-column-offset(1);
  @include make-md-column(7);
  @include make-md-column-offset(1);
  height: 100%;
  padding-top: 90px;
}

.card-row {
  @include make-row();

  .card-container-small, .card-container-large {
    margin-bottom: 30px;
  }

  .card-container-small {
    @include make-md-column(4);
  }

  .card-container-large {
    @include make-md-column(8);
  }
}

.card {
  @include transition-all-ease;
  @include shadow(1);
  background-color: $background-color;
  border-radius: $border-radius;
  height: 300px;
  padding: 15px;
  position: relative;
  overflow: hidden;

  .card-header-container {
    bottom: 0px;
    position: absolute;
    right: 0px;
  }

  .project-category, .project-title {
    @include transition-all-ease;
    background-color: rgba(0, 0, 0, 0.5);
    clear: right;
    color: $white;
    display: inline-block;
    float: right;
    padding: 6px 12px;
    text-align: right;
    //visibility: hidden;
  }

  .project-category {
    @extend .h4;
    margin: 0;
    margin-bottom: 6px;
    font-family: $bodyFontFamily;
    font-weight: $font-weight-light;
  }

  .project-title {
    @extend .h3;
    margin: 0;
    font-family: $headerFontFamily;
    font-weight: $font-weight-bold;
  }

  &:hover {
    @include shadow(3);
    cursor: pointer;

    .project-category, .project-title {
      visibility: visible;
      width: auto;
    }
  }
}

// Individual Project Thumbnails

@mixin project-thumb($imgpath) {
  background-image: url($imgpath);
  background-repeat: no-repeat;
  background-size: cover;
}

#project-color {
    @include project-thumb("<%= asset_path("thumb_color.png") %>");
    background-position: top;
}

#project-foodshift {
    @include project-thumb("<%= asset_path("thumb_foodshift.png") %>");
    background-position: center;
    background-size: contain;
}

#project-layout {
    @include project-thumb("<%= asset_path("thumb_layout.png") %>");
    background-position: left;
}

#project-odalc {
    @include project-thumb("<%= asset_path("thumb_odalc.png") %>");
    background-position: center;
    background-size: contain;
}

#project-typography {
    @include project-thumb("<%= asset_path("thumb_typography.png") %>");
    background-position: center;
}

#project-art-thieves {
    @include project-thumb("<%= asset_path("thumb_art_thieves.png") %>");
    background-position: center;
    background-size: contain;
}
